<body background="https://s3.amazonaws.com/kickthelist/media/images/CityScape.jpg">
    
  <font color="white">
    <%= form_for @pin, html: { multipart: true } do |f| %>
      <% if @pin.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

          <ul>
          <% @pin.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

        <div class="form">
          <div class="form-group">
              <!-- <%= f.label :add_to_your_list %> -->
              <%= f.text_field :description, autofocus: true, class: "form-control", id:"exampleInputText3", placeholder:"Example: Travel across all 7 continents" %>
            </div>

           <div class="form-group">
              <%= f.label :Upload_photo_if_complete %>
              <%= f.file_field :image, class: "form-control" %>
            </div> <br><br>

            <div class="form-group">
              <%= f.submit class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
  </font>
</body>