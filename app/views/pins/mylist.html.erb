<body background="https://s3.amazonaws.com/kickthelist/media/images/CityScape.jpg">


<%= render 'pages/home' unless user_signed_in? %>


<% if user_signed_in? %>

<div class="center">
<font color="white"><h2> Hi, <%= current_user.name %> <p> #KickTheList</h2> <br></font>
<!-- <p> Add awesome stuff to your list. Kick the things you've done! <p></font> --> 

<%= link_to "Add to your list", new_pin_path, class: "btn btn-success" %></div> <br><br>
        

<div id="pins" class="infinitescroll-enabled">
          
  <% @pins.each do |pin| %>
    <div class="box panel panel-default">
      <div class="panel-body">
          <div class= "media">
              <div class="media-left">
                  <!--<%= image_tag pin.user.image.url(:medium) %> 
                  <strong><%= pin.user.name if pin.user %></strong> -->
              </div>
                   <div class="media-body media-middle", style="padding-left: 10px">
                      <%= link_to pin.description, pin %>
                      <!-- <% if pin.user == current_user%>
                        <div class="actions">
                          <%= link_to 'kick', edit_pin_path(pin)%> 
                        </div>
                      <% end %> -->
                    </div>
          </div>
      </div>
    </div>  
<% end %>
</div> 

<% else %>
<%end%>

<div class="center">
   <%= will_paginate @pins, renderer: BootstrapPagination::Rails %>
</div>

</body>